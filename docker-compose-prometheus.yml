version: '3'

services:
  prometheus:
    container_name: prometheus
    image: prom/prometheus
    restart: always
    ports:
      - '9090:9090'
    # volumes:
    #   - ./volumes/prometheus:/etc/prometheus